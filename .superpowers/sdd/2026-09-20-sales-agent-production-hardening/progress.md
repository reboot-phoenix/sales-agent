# SDD ledger — plan: docs/superpowers/plans/2026-09-20-sales-agent-production-hardening.md (extended: arsenal + compliance pass 2)

- Pre-flight: shared interfaces: LEAD_SELECT_SQL consumed by leads list/export (adding columns must keep parity test green on both sides); scoring.ts toScore10 consumed by leads.ts score endpoint + web freshness.ts mirror (mapping must match); SCRAPER_MAP consumed by army_registry FAILOVER + scheduler + test_real_integration (>=15 sources, EXTRA membership). No conflicts found.
- Ruling: new sources must have webfetch-verified endpoints before coding — no fabricated APIs — cost if wrong: dead soldiers tripping breakers.
- Ruling: freshness stored as GENERATED column on job_postings (leads can't reference jp.posted_at in generated expr) — cost if wrong: migration failure on cross-table reference.
- Ruling: engine stays 0-100 internally; 1-10 is product presentation everywhere in UI — full rescale would break scoring parity tests + bands; cost if wrong: 96-test suite red.
- Task A (new soldiers): complete. hackernews.py + ripplehire.py + freshersvoice site + map/defaults/failover + docs. Tests: test_new_soldiers.py 14/14. Full scrapers suite 472 pass (1 live-network deselect).
- Task B (stored freshness): complete. Migration 012 + schema col + write-time CASE (normalizer INSERT + 2 UPDATEs, importLeads INSERT + merge) + scheduler refresh_freshness + LEAD_SELECT_SQL stored-first. Ruling: CASE IS NULL branch LAST — Postgres types unknown params from first typed context, leading IS NULL throws AmbiguousParameterError; verified by repro. Fixed in all 5 SQL sites.
- Task C (1-10 display): complete. LeadDetail badge/KV/explanation + MyLeads cell show x/10 with /100 in title. API 164/164, web 25/25 + vite build ok, compose config ok.
- Ruling: Keka/Darwinbox/Freshteam skipped (no verifiable board pattern); YC jobs cut (JS SPA, no stable JSON); Hirect cut (origin transport error). Dork discovery covers them instead of fabricated URLs.
- Task D (stop army + global live status): complete. Workers POST /army/stop (halt flag TTL 600s, backlog clear, restart-override on new run); consumer honors halt pre-job + mid-run cancel loop, suppresses fallback when stopped; API proxy + audit; UI Run<->Stop toggles on Dashboard/Leads + global header pill on every page. Tests: test_army_stop.py 5/5. Browser-verified: stop confirm -> halt flag set (TTL 575s observed), hero/pill flip, claim/verify dialogs, no overflow at 390/768/1280.
- Cron verdict (evidence): scheduler enabled, fired exactly 2026-09-20 03:00:07 UTC; 61 leads created/24h; breakers all 0 failures. 03:00 tick recorded 0 leads_found (cold-start right after image rebuild); adjacent runs collected thousands. Mechanism proven; yield varies per tick.
- Suites: scrapers 477, api 164, web 25 + build ok.
- Task E (modern charts): complete. Shared kit charts/{theme,ChartTip,Donut,TrendArea,HBars}; Dashboard trend/funnel/coverage/bands/verif/outreach migrated; Analytics gained conversion bars, verif/outreach donuts, credit donut, runs mini-bars, clickable funnel rows. Browser-verified: 5+4 SVG surfaces render, funnel click deep-links, no page errors, no overflow. web tsc+25/build ok.
- Task E2 (chart polish from screenshots): complete. Semantic resultColor for verif/outreach donuts; StatCardGrid auto-fit (no orphan wrap); trend X tick dy fix; Donut column layout for narrow cards; HBars grow-to-fill + dynamic bar sizing; funnel card h-full flex; conversion null-filter + run-id truncation. Screenshot-verified at 1440 + 390, no overflow.
- Task F (wafer.ai design language): complete. Analyzed wafer.ai full site (screenshots + tokens: Space Grotesk/DM Sans/Space Mono, 56px/500/-0.02em display, #0A0A0A dark, mono micro-caps eyebrows with status dot, full pills, hairline bordered cards, thin-line dotted-grid mono-label dataviz). Ported as light-theme accents: pill buttons, mono eyebrow class, PageHeader eyebrow prop (7 pages), softer card/menu/toast/select radii, mono chart ticks + dotted grids + trend node dots + mono tooltips + mono freshness/HBar labels. Screenshot-verified Leads + Analytics at 1440. web tsc+25 ok.
- Task G (full-field filters): complete. Backend contact/freshness params + extended global search (6 new API tests); frontend toolbar adds Source/City/Department/Contact/Date + server-side freshness (removed lying client-side page filter). Verified live: 975 baseline -> none 433, enriched 516, fresh 9, Bengaluru 7. Suites: api 170, web 25.
